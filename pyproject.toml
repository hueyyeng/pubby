[project]
name = "pubby"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Huey Yeng", email = "huey.yeng.mmu@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "ffmpeg-python>=0.2.0",
    "jinja2>=3.1.6",
    "pyside6>=6.11.0",
]

[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"

[project.scripts]
pubby = "pubby.__main__:main"
